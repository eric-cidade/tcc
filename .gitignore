# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Environments
.venv/
venv/
ENV/

# Databases and Search Engines (Crucial para o seu projeto)
meili_data/
chroma_db/
*.local

# Índice exportado para o servidor (deploy/exportar_indice.py) e o pacote de
# envio. São artefatos gerados, de ~91MB e ~59MB — nunca vão para o histórico.
chroma_db_export/
*.tar.gz

# Caches do servidor: a unit do systemd aponta HF_HOME para cá, então os pesos
# do modelo (~2,3GB) e o cache do uv ficam dentro do diretório do projeto.
.cache/

# Secrets and local configs
.env
.flaskenv

# OS generated files
.DS_Store
.DS_Store?
._*
Thumbs.db
desktop.ini
/graficos
# Data (Opcional: descomente se não quiser subir seus .txt)
# /csv/ht/original/
# /csv/ht/simplificada/